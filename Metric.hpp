#ifndef DEANN_METRIC
#define DEANN_METRIC

/**
 * @file
 * This file provides a metric enum.
 */

namespace deann {
  /**
   * A simple enum for marking the metric.
   */
  enum class Metric {
    EUCLIDEAN, TAXICAB
  };
}

#endif // DEANN_METRIC
